import { ValidationClassPipe } from './validation-class.pipe';

describe('ValidationClassPipe', () => {
  it('should be defined', () => {
    expect(new ValidationClassPipe()).toBeDefined();
  });
});
